# BraincCareDZ — Session Summary

## Objective
- Améliorations de schéma appliquées : renommage factures→invoices (point 11), montants NUMERIC+CHECK (point 3), suppression `appointments.session_note_id` (point 6), `patient_motifs.id` PK (point 9), `professions`/`motifs.is_active` (point 10), CHECK constraints sur les statuts (point 4), **automatisation des packs** (point 5, option B retenue).
- **Règle praticien** : `patients.practitioner_id` rendu obsolète — praticien courant dérivé du dernier RDV non annulé, historique via les RDV, tous voient tout. ✅ TERMINÉ (migration + backend + tests).
- **Filtrage multi-critères** sur 5 listes (patients, RDV, paiements, factures/entreprises, dépenses), côté client. ✅ TERMINÉ.
- **Refonte listes (session en cours)** : FilterDialog en **panneau dépliant pleine largeur** (sous le header, au-dessus de la liste), **tri par clic sur les en-têtes** (colonne active soulignée), **total des paiements réactif aux filtres**, **filtre ajouté sur Services**. ✅ TERMINÉ.
- **Nettoyage schéma (session en cours)** : suppression de `users.is_active`, `users.specializations` et `patients.status` (DB + backend + frontend) ; rôles affichés **Admin / Psy / Coach** (valeurs techniques `admin`/`practitioner`/`receptionist` conservées). ✅ TERMINÉ (sauf build final à relancer).
- Diagramme `docs/database-uml.html` réagencié : **19 tables, 24 relations**, aucune superposition. ✅ TERMINÉ (mis à jour : users 8f, patients 36f).
- **Formulaires RDV (session récente)** : création corrigée (sélection patient conservée, praticien pré-rempli) + **règle de conflit par rôle+patient** (2 RDV possibles dans le même créneau si patient et/ou rôle différents — calendriers Admin/Psy/Coach). ✅ TERMINÉ.
- **Liste patients (session récente)** : icône solde insuffisant (bal < prix/séance pack courant) + tooltip, pack affiché `(remaining/total)`. **Paiements** : total en bas de liste + filtre Période (aujourd'hui/semaine/mois/trimestre/année). **3 calendriers** (Admin/Psy/Coach) avec toggle admin. ✅ TERMINÉ.

## Important Details
- Contrainte utilisateur : **pas de superposition/collinéarité** entre relations ; les croisements ponctuels ("croisements") sont acceptables.
- Solution : **routage manuel des 24 edges**, vérifié par script.
- **Layout injecté** : users centré (520,80) ; patients en colonne gauche avec ses **36 champs complets** ; patient_pack_usages (760,920,w300,junction) ; viewBox `0 0 1960 1960`.
- Schéma `patients` (36 colonnes, ordre DB, `profession_id` en dernier) récupéré depuis la base live via `information_schema` (après suppression de `status`).

## Migrations appliquées sur la base live (BrainCare, postgres/N@zim2002)
- `sql/refine_schema.sql` : factures→invoices, facture_items→invoice_items, facture_id→invoice_id, designation→description, total_ht→subtotal, total_discount_ht→discounted_total, vat→vat_amount, total_ttc→grand_total ; NUMERIC(12,2)+CHECK ; DROP appointments.session_note_id ; patient_motifs id UUID PK + UNIQUE(patient_id,motif_id) ; professions/motifs is_active ; renommage NOT NULL/contraintes (invoices_*, invoice_items_*, payments_patient_id_fkey, idx_payments_patient_id).
- `sql/status_checks.sql` : CHECK sur users.role, patients.status/patient_type/marital_status, services.type, appointments.status, payments.status/payment_method. Correction au passage d'une valeur corrompue `Mari�(e)`→`Marié(e)`.
- `sql/pack_automation.sql` : table `patient_pack_usages` (pack_id, appointment_patient_id UNIQUE) + trigger `trg_pack_sync_status` sur appointments.status → décrémente le pack FIFO (patient+service) au passage en 'completed', rend la séance si retour en arrière. Endpoint manuel `POST /patient-packs/:id/use` supprimé (code mort) ainsi que `patientPacks.useSession` dans `lib/api.ts`. Testé : 5→4 en completion, retour 4→5.
- `sql/patient_outcomes.sql` : table `patient_outcomes` (patient_id, evaluated_at, perceived_improvement, observed_changes, global_satisfaction, would_recommend, created_by) = historique des évaluations. Backfill : 11 lignes depuis les champs patients. Endpoints `GET /api/patient-outcomes/patient/:patientId`, `POST /api/patient-outcomes`, `DELETE /api/patient-outcomes/:id`. `patients.ts` : helper `recordOutcome` insère une ligne au POST/PUT quand des champs d'évaluation sont présents. Frontend : page détail patient → section Évaluations (liste + formulaire + suppression).
- `sql/pack_price.sql` : colonne `patient_packs.price` NUMERIC(12,2) NOT NULL (backfill depuis services.price). Modèle métier : le patient paye librement (avance) ; chaque séance faite débite. Le `balance` de `patients.ts` déduit désormais `price / total_sessions` si la séance a consommé un pack (via `patient_pack_usages`), sinon `services.price`. `patient-packs.ts` POST accepte `price` (fallback services.price). Le point 13 (lier payments au RDV) est écarté — le modèle est « avance libre débitée par les séances », pas paiement-par-séance.
- `sql/patient_balance.sql` : colonne `patients.balance` NUMERIC(12,2) NOT NULL DEFAULT 0 (solde stocké, dénormalisé). Fonction `recalc_patient_balance(p_id)` + triggers : `trg_payment_balance` (payments INSERT/UPDATE/DELETE), `trg_usage_balance` (patient_pack_usages INSERT/DELETE), et le trigger pack existant (`sync_pack_on_appointment_status`) recalcule le solde des patients du RDV à chaque changement de statut. `patients.ts` : `balance` lu depuis la colonne stockée (`c.balance`) au lieu du calcul en ligne. Testé : paiement +3000 → solde 3000 ; RDV complété (service 3000) → 0 ; revert → 3000 ; suppression paiement → 0. UML : patients passe à 37 champs (balance), h=784, edges bas patients ajustés y844→864.
- `sql/drop_user_active_spec.sql` : `ALTER TABLE users DROP COLUMN IF EXISTS is_active`, `DROP COLUMN IF EXISTS specializations`. Appliqué.
- `sql/drop_patient_status.sql` : `ALTER TABLE patients DROP COLUMN IF EXISTS status` (CHECK `patients_status_check` auto-supprimé avec la colonne), `DROP INDEX IF EXISTS idx_clients_status` (n'existait pas → NOTICE, sans effet). Appliqué.

## Final Layout (tables; h = 34 + fields*20 + 10)
- users (520,80,w300,8f) · patients (60,80,w300,36f,h764) · services (1260,60,w280,11f) · practitioner_services (980,80,w240,3f,junction)
- professions (520,420,w240,5f) · motifs (520,640,w220,5f) · patient_motifs (820,560,w260,4f,junction)
- appointments (1240,420,w300,10f) · appointment_patients (1240,780,w260,4f,junction) · session_notes (1240,1000,w340,6f)
- payments (1660,420,w280,8f) · patient_packs (60,940,w280,9f) · patient_pack_usages (760,920,w300,4f,junction) · expenses (60,1260,w260,9f) · activity_logs (60,1600,w340,15f)
- companies (700,1240,w260,9f) · invoices (1040,1260,w300,13f) · invoice_items (1040,1620,w300,7f)
- patient_outcomes (340,1260,w300,9f)
- bbox réel : x8–1940, y8–1944 → viewBox `0 0 1960 1960`.

## Final 24 Edges (pts in HTML)
1. users→practitioner_services `[[820,130],[980,130]]`
2. services→practitioner_services `[[1260,130],[1220,130]]`
3. users→appointments `[[820,250],[1240,250],[1240,500]]`
4. services→appointments `[[1400,324],[1400,420]]`
5. services→patient_packs `[[1400,60],[1400,8],[8,8],[8,980],[60,980]]`
6. users→patient_packs `[[600,80],[600,12],[20,12],[20,1020],[60,1020]]`
7. patients→patient_packs `[[200,844],[200,940]]`
8. patient_packs→patient_pack_usages `[[340,1000],[760,1000]]`
9. appointment_patients→patient_pack_usages `[[1240,900],[1060,900],[1060,920]]`
10. appointment_patients→session_notes `[[1260,904],[1260,1000]]`
11. users→session_notes `[[700,80],[700,25],[1600,25],[1600,1050],[1580,1050]]`
12. patients→payments `[[260,844],[260,910],[1700,910],[1700,624]]`
13. users→expenses `[[640,80],[640,24],[32,24],[32,1340],[60,1340]]`
14. users→invoices `[[760,80],[760,35],[1620,35],[1620,1320],[1340,1320]]`
15. users→activity_logs `[[720,80],[720,44],[44,44],[44,1700],[60,1700]]`
16. companies→invoices `[[960,1320],[1040,1320]]`
17. invoices→invoice_items `[[1140,1564],[1140,1620]]`
18. appointments→appointment_patients `[[1260,664],[1260,780]]`
19. patients→appointment_patients `[[360,800],[1240,800]]`
20. professions→patients `[[520,480],[360,480]]`
21. patients→patient_motifs `[[360,600],[820,600]]`
22. motifs→patient_motifs `[[740,700],[820,700],[820,664]]`
23. patients→patient_outcomes `[[440,844],[440,1220],[340,1220],[340,1260]]`
24. users→patient_outcomes `[[520,180],[430,180],[430,1260]]`

## Design (stubs/corridors du nouveau layout)
- **5 stubs verticaux distincts sur le top de users** (x600, 640, 700, 720, 760) rejoignant les corridors horizontaux y12/24/25/35/44.
- Corridors gauches : x8/20/32/44 (tous < x60 de patients, donc aucune traversée).
- Corridor droit : x1600/1620 entre session_notes (x2=1580) et payments (x=1660).
- patients→payments entre par le **bas** de payments (1700,624) ; entrées patient_packs : top (200,940), gauche (60,980), gauche (60,1020).
- patient_pack_usages : entre patient_packs et appointment_patients, à (760,920,w300,h124) ; edges #9 (340,1000)→(760,1000) et #10 (1240,900)→(1060,900)→(1060,920).
- patient_outcomes : entre patients et le corridor x430 (sortie users côté gauche y180) ; edge #24 (520,180)→(430,180)→(430,1260) entre par le top de patient_outcomes.

## Verification (checker.cjs à jour, données = HTML)
- boxCross: **0** · collinear: **0** · pointCrossings: **25** · bbox `8 8 1940 1944`
- tables: 19, edges: 24, users fields: 8, patients fields: 36

## Work State
### Completed
- Migrations refine_schema.sql, status_checks.sql, pack_automation.sql, patient_outcomes.sql, pack_price.sql, patient_balance.sql, practitioner_rule.sql, drop_user_active_spec.sql, drop_patient_status.sql appliquées + vérifiées (état BD confirmé).
- Règle praticien : `patients.practitioner_id` nullable + FKs supprimées ; `baseSelect` de patients.ts réécrit (LATERAL `cur_prac` = dernier praticien, `prac_list` = historique json_agg, alias corrigés). GET /appointments : filtre `a.practitioner_id = req.user.id` retiré (les non-admins ne voyaient que 0 RDV) — tous voient tout. Tests API : 11 patients, 5 users, 3 praticiens ; création RDV avec practitionerId, PUT changement de praticien OK, RDV de test nettoyé.
- Filtrage multi-critères (côté client) : composant `components/filter-dialog.tsx` (`FilterDialog`, `FilterField`, `FilterValues`, `countActiveFilters`, `resetFilters` ; types text/select/date-range/number-range). Intégré à Patients (nom/email/tel, praticien, pack, séances, solde — **statut patient retiré**), Rendez-vous (statut, praticien, service, période — liste + calendrier via findAppointmentsForSlot), Paiements (patient, mode, statut, montant, date), Factures (référence, entreprise, date, total) + Entreprises (nom, propriétaire, adresse, RC, NIF, NIS), Dépenses (libellé, catégorie, créé par, montant, date). Packs ignorés (décision utilisateur). Bugs TDZ corrigés dans company-invoices/expenses (usePagination référencée avant déclaration).
- **Refonte listes** : `FilterDialog` passé en **panneau dépliant pleine largeur** (fragment `<>` + `basis-full w-full`), **sorti du flex header** et placé en bloc `w-full` entre le header et la liste sur les 5 pages (patients, appointments, payments, expenses, company-invoices). **Tri par colonnes** : nouveau `components/sortable-header.tsx` (`useSort(items, accessors, initialKey)` + `SortableHeader` ; colonne active soulignée `underline decoration-2 underline-offset-4` + flèche asc/desc). Appliqué à patients, services, payments, expenses, company-invoices (2 tables `invoiceSort`+`companySort`), admin (users + activity logs). `filteredX` → `sortedItems` avant `usePagination`. **Total paiements** : `totalFiltered` = somme des `amount` de `filteredPayments`, affiché dans le CardHeader (« Total (tous|filtré) » + montant DZD). Filtre ajouté sur **Services** (remplace la barre de recherche : nom, type, séances, prix). Vue liste RDV = cartes (pas de table) → pas de tri. Logs admin paginés serveur (50/page) → tri sur la page chargée. Ne pas ajouter de totaux sur les autres pages (non demandé). `next build` OK ; **attention** : `npm run build` peut échouer par intermittence sur le téléchargement des Google Fonts (geist/rozha_one dans app/layout.tsx) — problème réseau transitoire, relancer.
- **Suppression users.is_active + specializations** : DB (drop_user_active_spec.sql) ; backend users.ts (GET liste, GET/:id, /practitioners sans `WHERE is_active = TRUE`, POST, PUT, reset password), auth.ts (login + /me), create-admin.ts, seed.ts nettoyés. Frontend : admin page sans colonne Statut ni sélecteur isActive (interface, editFormData, accessor sort, handleEditUser, table, dialog). **Rôles affichés Admin / Psy / Coach** (valeurs techniques conservées) : `getRoleLabel` + SelectItems des dialogs admin + register page (`app/auth/register/page.tsx`).
- **Suppression patients.status** : DB (drop_patient_status.sql, CHECK auto-supprimé) ; backend patients.ts (baseSelect, POST réécrit `$1..$29`, PUT renuméroté) ; frontend patients page (interface, filtre status, case de filtrage), patients/[id] (interface + bloc Statut), patient-form.tsx (`status?: string` retiré de PatientFormData — aucun input n'existait). `abandon_reason` conservé.
- UML mis à jour : users 8f, patients 36f (h764), edges bas patients y864→844 ; checker.cjs à jour (users 8, patients 36) ; vérifié 0 boxCross, 0 collinear, 25 pointCrossings.
- Backend : company-invoices.ts (renames + champs), appointments/session-notes/seed (session_note_id retiré), settings.ts (is_active + soft-delete), patients.ts (réactivation is_active, recordOutcome, balance par pack, cur_prac), patient-packs.ts (use supprimé, price accepté), patient-outcomes.ts (nouveau : GET/POST/DELETE), server.ts (CREATE patient_pack_usages + trigger + patient_outcomes + balance/triggers ; **bloc « Drop CHECK constraints patients/clients » SUPPRIMÉ** — il annulait status_checks.sql au démarrage).
- Vérification exhaustive de la base live : 22/22 ✅ (tables renommées, CHECK x21, triggers x3, patient_pack_usages, patient_outcomes 11 lignes, patient_packs.price NOT NULL, patients.balance NOT NULL + cohérence 0 mismatch, patients.practitioner_id nullable sans FK).
- Frontend : company-invoice-form.tsx, company-invoice-pdf.ts, company-invoices/page.tsx (renames + filtres), appointments/page.tsx (sessionNoteId retiré + filtres), lib/api.ts (useSession retiré, patientOutcomes ajouté), patients/[id]/page.tsx (section Évaluations), payments/page.tsx + expenses/page.tsx (filtres + tri), filter-dialog.tsx (nouveau), sortable-header.tsx (nouveau).
- HTML UML : **19 tables / 24 edges** (edge users→patients `practitioner_id` retiré car la FK n'existe plus ; edge users→patient_outcomes `created_by` ajouté), géométrie validée (0 boxCross, 0 collinear, 25 pointCrossings, bbox 8 8 1940 1944).
- **Formulaires RDV** : `appointment-form.tsx` — `handleInputChange('serviceId')` ne vide plus `patientIds` (tronque à 1 si service non-neurofeedback) ; praticien pré-rempli = utilisateur courant s'il est praticien, sinon premier praticien. **Règle de conflit (backend/routes/appointments.ts)** : POST + PUT — conflit si **même rôle** (via `users.role`) **ET au moins un patient commun** (`appointment_patients`) ; `/availability/:date` filtré par rôle. Testé : 2 RDV même créneau patients différents → OK ; même patient+même rôle → 400 ; RDV de test nettoyés. **Frontend calendrier** : bouton « + » sur créneaux occupés pour ajouter un RDV supplémentaire (autre patient/calendrier).
- **Liste patients** : icône AlertCircle rouge quand `balance < packPricePerSession` (lateral `pack_next` de patients.ts calcule `price/total_sessions`, fallback `services.price/sessions` ; exposé `packPricePerSession`) + Tooltip « Solde insuffisant pour une séance » ; pack affiché `(remaining/total)`. **Paiements** : total en bas de liste (après pagination) + filtre Période (`periodToRange`). **Calendriers** : backend ajoute `practitionerRole` à chaque RDV ; frontend état `calendarRole` + toggle Admin/Psy/Coach (admin seul), filtrage par rôle dans `findAppointmentsForSlot`/`filteredAppointments`.
- **Règle occupation créneau par rôle (backend + formulaire)** : Psy (`practitioner`) = **1 RDV par créneau** (bloque même un autre patient) ; Coach (`receptionist`) = **jusqu'à 4 RDV par créneau** (bloque le 5e) et **jusqu'à 4 patients par RDV** (même service non-neurofeedback) ; Admin = traité comme Psy (1). `/practitioners` renvoie tous les utilisateurs (avec `role`) ; formulaire pré-sélectionne le praticien du rôle de calendrier actif (`preferredRole`), affiche le rôle dans le dropdown, et autorise multi-patients (max 4) quand le praticien est Coach (sinon 1 sauf neurofeedback). Testé : Psy 1er OK / 2e patient bloqué 400 ; Coach 4 RDV OK / 5e bloqué 400 ; Coach 3 patients sur service non-neuro OK ; nettoyé.
- **Praticien par calendrier (session récente)** : Psy → praticien sélectionné à l'avance ; Coach → **aucun praticien à la création** (RDV créé sur le calendrier Coach = appartient au rôle `receptionist`). Migration `sql/appointment_role.sql` : `appointments.practitioner_id` devient **NULLable** + nouvelle colonne `appointments.role` (rôle du calendrier cible). Backend POST/PUT : `role='receptionist'` → `practitioner_id = NULL` ; conflit par `COALESCE(u.role, a.role)` ; `appointmentSelect` en `LEFT JOIN users` avec `COALESCE(u.name,'Non assigné')` et `practitionerRole = COALESCE(u.role, a.role, 'receptionist')`. **Session-notes POST** : si le RDV Coach n'a pas de praticien, l'utilisateur qui rédige le compte rendu **devient le praticien en charge** (`UPDATE appointments SET practitioner_id = req.user.id, role = req.user.role`). Formulaire : pas de pré-sélection ni de champ praticien pour `preferredRole === 'receptionist'` (message « Aucun praticien n'est assigné »), `editFromAppointment` repasse `preferredRole='receptionist'` si `practitionerId` absent. Testé : création RDV Coach sans praticien OK (role receptionist), 2 RDV Coach même créneau OK, Psy même créneau OK, note sur RDV Coach non assigné → praticien = auteur (admin/7e243b87) ; nettoyé.
- **Pack différé (session récente)** : pour un nouveau patient (premier RDV), le pack peut être sélectionné **après** la première séance. Migration `sql/pack_deferred.sql` : colonne `appointments.pack_deferred BOOLEAN NOT NULL DEFAULT FALSE`. Backend : `appointmentSelect` expose `packDeferred` + `packSelected` par patient (EXISTS patient_pack_usages) ; POST/PUT acceptent `packDeferred`. Nouvel endpoint `POST /patient-packs/select` (patientId, serviceId, totalSessions, price?, appointmentPatientId) : valide le junction, **409** si usage déjà existant, réutilise un pack existant (patient+service, remaining>0) sinon le crée, insère `patient_pack_usages`, décrémente `remaining_sessions` de 1, logActivity, renvoie le pack. Frontend : checkbox « Sélectionner le pack après la séance » dans `appointment-form.tsx` ; page rapport (`appointments/[id]/report/page.tsx`) ouvre une **popup obligatoire** (non contournable : `showCloseButton={false}`, `onInteractOutside`/`onEscapeKeyDown` preventDefault, retour bloqué tant que pack non sélectionné) pour choisir le pack → `patientPacks.selectForSession`. `lib/api.ts` : `selectForSession` ajouté. Testé de bout en bout : RDV `packDeferred=true` créé, complété sans débit (packSelected=false), `POST /select` → pack créé remaining 1→0, 2e appel → 409, solde patient recalculé (16020→13020), nettoyage → solde restauré 16020. ✅ TERMINÉ.
- **Pack différé : service optionnel (session récente)** : quand on coche « Sélectionner le pack après la séance », le champ **Service est désactivé et non obligatoire** (le service est choisi dans la popup de pack après la séance). Migration `sql/deferred_service_null.sql` : `appointments.service_id` devient **NULLable**. Backend `appointments.ts` : `appointmentSelect` en `LEFT JOIN services` (serviceName/servicePrice NULL possibles) ; POST — `serviceId` requis **sauf** si `packDeferred` (validation custom), vérification service conditionnelle, INSERT avec `serviceId || null` ; PUT — `nextServiceId = packDeferred ? null : serviceId ?? current.serviceId` (UPDATE direct sur service_id, plus de COALESCE). `server.ts` : `recalc_patient_balance` en `LEFT JOIN services` + `COALESCE(s.price, 0)` pour que les séances à service NULL soient comptées (débit via pack) et non ignorées. Frontend `appointment-form.tsx` : `Select Service` désactivé si `packDeferred` (+ libellé « optionnel — choisi après la séance », placeholder « Service sélectionné après la séance »), cocher vide `serviceId`, validation service requise seulement si pas `packDeferred`. Page rapport : popup pack avec **Select Service** si le RDV n'a pas de service (sinon Input readOnly), `handleSelectPack` utilise `appointment.serviceId || packServiceId`. Page RDV : affichage « Pack à définir » quand `packDeferred` sans service (carte + calendrier), types `serviceId`/`serviceName` nullable. Testé : RDV packDeferred créé sans service (serviceId null) → complété sans débit → `POST /select` avec service choisi → pack créé (Consultation classique, remaining 1→0), solde 16020→13020 ; nettoyage → 16020. ✅ TERMINÉ.
- **Popup pack : sélection directe du pack (session récente)** : la popup de sélection du pack (page rapport) ne demande plus de saisir manuellement le nombre de séances ni le prix — on **choisit un pack (service)** dans la liste, et le **prix + nombre de séances sont déduits automatiquement** (`totalSessions = service.sessions`, `price = service.price`). États `packTotalSessions`/`packPrice` supprimés ; `selectedPackService` (useMemo) affiche « Nombre de séances » et « Prix du pack » en résumé. `handleSelectPack` garde `appointment.serviceId || packServiceId`, exige un pack valide. Testé : RDV packDeferred → popup avec liste des packs → sélection Consultation classique → pack créé total=1 price=3000 remaining=0, solde 16020→13020, nettoyage → 16020. ✅ TERMINÉ.
- **Pack choisi = pack en cours + débit d'une séance (session récente)** : à la sélection du pack (popup après « Enregistrer » du compte rendu), le pack choisi devient le **pack en cours** du patient (patients.ts l'expose via `pack_next`/`pack_list` basés sur `appointments.service_id` + comptage `completed`) et **une séance est déduite** (`remaining_sessions` = `total_sessions - 1`). Nouveau dans `backend/routes/patient-packs.ts` `POST /select` : après création du pack et insert de l'usage, **UPDATE du RDV** `appointments.service_id = serviceId` (seulement si `service_id IS NULL`) pour que le service choisi soit enregistré comme service en cours du RDV. Testé : RDV packDeferred sans service → complété (solde inchangé 16020) → `/select` neuro8 (8 séances, 33000) → pack total=8 remaining=**7**, RDV `service_id`=NeuroFeedback8, solde 16020→11895 (16020−33000/8=4125/séance), nettoyage → 16020. ✅ TERMINÉ.
- **Popup pack : ouverture + séances restantes (session récente)** : suppression du `useEffect` qui ouvrait la popup automatiquement au chargement de la page rapport — elle ne s'ouvre **qu'au clic « Enregistrer »** (`handleSaveAll`) et en garde-fou à la sortie (`handleGoBack`). Le résumé de la popup affiche désormais « Nombre de séances » (total) ET « Séances restantes » (total − 1, la séance déjà consommée avant le choix). ✅ TERMINÉ.
- **Patients liste : pack_remaining depuis patient_packs (session récente)** : `backend/routes/patients.ts` `pack_next`/`pack_list` priorisent **`patient_packs.remaining_sessions`** (source de vérité débitée par le trigger et `/select`) via `LEFT JOIN LATERAL` sur le pack le plus récent (remaining>0) du patient+service, avec repli sur l'ancien calcul `sessions − COUNT(completed)`. Frontend liste patients affiche déjà `packRemaining/packTotal` (aucun calcul client). Testé : RDV packDeferred → /select neuro8 → pack 7/8 → liste patients « NeuroFeedback8 7/8 », nettoyage → 16020. ✅ TERMINÉ.
- **Copie réelle = DZ (un seul projet)** : `C:\Users\Latitude\Documents\BrainCareDZ` est l'unique copie. `C:\Users\Latitude\Videos\BC2` (miroir) et `C:\Users\Latitude\BrainCareDZ` (ancienne version) ont été **supprimés** sur demande. Plus de sync nécessaire. Serveurs DZ (ports 3000/5001) actifs. ✅ TERMINÉ.
- Points restants : 1 (EXCLUDE double-booking), 12 (email/pg_trgm) ; 13 écarté (modèle avance libre) ; 14 sans action.

### Active
- **Nettoyage données de test (session récente)** : supprimés les RDV pack-deferred de test liés à nazim (`0996b4ad`, `7aa601bf`, `157c83bc`, `4f353a8f`, `5d04e1ba`, `c2aa1c77`) + pack `696452ca` ; **solde nazim restauré à 16020.00** (baseline), `session_count`=1, `last_session_date`=2026-04-20. Vérifié : 0 usage orphelin. **Le double débit n'est plus reproductible** (garde `IF EXISTS usage THEN CONTINUE` présente en base ET dans server.ts, mais **travail NON COMMITÉ** — commit `8e7a623` n'a pas du tout le trigger pack). Nettoyage demandé par l'utilisateur = **nettoyer seulement** (pas de commit).

### Blocked
- (aucun — reste la décision de commiter ou non la correction trigger)

## Next Move
- Nettoyage des données de test demandé : **fait** (pack 696452ca + RDV test nazim supprimés, solde 16020 restauré). Il reste des patients de test avec packs (`Test Patient`, `test2 testt`, `test test`, `Rayan MOUZAOUI`, `test3 test`) — à nettoyer si souhaité. Décision en attente : **commiter** la correction du trigger pack (actuellement non commitée).

## Relevant Files
- `docs/database-uml.html` : layout + edges finaux (à ne plus modifier sauf demande).
- `sql/refine_schema.sql` · `sql/status_checks.sql` · `sql/pack_automation.sql` · `sql/patient_outcomes.sql` · `sql/pack_price.sql` · `sql/patient_balance.sql` · `sql/practitioner_rule.sql` : migrations appliquées (idempotentes en partie).
- `sql/drop_user_active_spec.sql` · `sql/drop_patient_status.sql` : suppressions de colonnes appliquées.
- `sql/appointment_role.sql` : `appointments.practitioner_id` NULLable + colonne `appointments.role` (calendrier cible).
- `sql/pack_deferred.sql` : colonne `appointments.pack_deferred` (pack sélectionné après la séance) — appliqué en base.
- `sql/deferred_service_null.sql` : `appointments.service_id` NULLable (service optionnel quand pack différé) — appliqué en base.
- `backend/routes/patient-packs.ts` : `POST /select` (création/réutilisation pack + débit d'une séance après la 1ʳᵉ).
- `components/sortable-header.tsx` : tri par colonnes (`useSort` + `SortableHeader`).
- `components/filter-dialog.tsx` : panneau de filtres dépliant pleine largeur.
- `app/(dashboard)/appointments/page.tsx` : calendrier (toggle Admin/Psy/Coach, « + » sur créneaux occupés) + liste RDV.
- `components/appointment-form.tsx` : formulaire RDV (patientIds conservés, praticien pré-rempli).
- `app/(dashboard)/patients/page.tsx` : icône solde insuffisant + tooltip, pack (remaining/total).
- `app/(dashboard)/payments/page.tsx` : total en bas + filtre Période.
- `backend/routes/appointments.ts` : occupation par rôle (Psy 1 / Coach 4 RDV par créneau), `practitionerRole`, `/availability/:date` par rôle, max patients par rôle.
- `app/(dashboard)/admin/page.tsx` : users (8f) + activity logs, rôles Admin/Psy/Coach.
- `C:\Users\Latitude\AppData\Local\Temp\opencode\checker.cjs` : vérificateur à jour (19 tables / 24 edges).
- `C:\Users\Latitude\AppData\Local\Temp\opencode\router.cjs` : essais A* abandonnés (obsolète).
- Backend (`server.ts`, `routes/*.ts`) : terminé, ne plus modifier sauf demande.
